name: Teste Projeto

on:
  workflow_run:
    workflows: ["Build Projeto"]
    types:
      - completed
    conclusion: success

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Executar Testes
        run: mvn test -Dsurefire.suiteXmlFiles=src/test/resources/testes/healthCheck.xml
